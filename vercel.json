{
  "framework": "nextjs",
  "buildCommand": "cd frontend && npm install && npx prisma generate && next build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install",
  "devCommand": "cd frontend && next dev"
}
